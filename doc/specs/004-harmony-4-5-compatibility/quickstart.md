# Quickstart: 双版本验收

1. 使用 DevEco Studio 同步工程并配置签名。
2. 分别连接 HarmonyOS 4.0 与 5.0 真机，安装同一 debug HAP。
3. 新建笔记、输入 Markdown、拍照插入，强制结束后重新打开。
4. 确认正文、图片和相对链接仍有效。
5. 分别连接 OneDrive 与测试 S3 Bucket，执行上传、下载、取消和冲突测试。
6. 检查日志不包含 token、secret key、预签名 URL 或个人文件内容。
