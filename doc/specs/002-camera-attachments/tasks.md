# Tasks: 拍照与本地附件

当前已完成项表示现有 HarmonyOS HAP 实现状态。Android APK 的对应实现由 `005`
跟踪，HAP 对齐由 `006` 跟踪；最终验收必须覆盖两端。

- [x] T201 通过 `ohos.want.action.imageCapture` 接入系统相机。
- [x] T202 将返回 URI 复制到本地附件目录。
- [x] T203 插入相对 Markdown 链接并显示最近照片预览。
- [x] T204 使用 UUID、真实 MIME 类型和正确扩展名命名附件。
- [x] T205 实现临时目录、校验、事务标记、原子提交和失败回滚。
- [x] T206 区分用户取消与真实错误。
- [x] T207 [P] 添加附件命名、路径、格式、插入位置和恢复决策单测。
- [x] T208 支持从编辑器重复拍照，并按当前光标/选区顺序插入。
- [x] T209 [P] 启动时仅回收未完成事务中且未被 Markdown 引用的附件。
- [x] T210 增加照片预览、直接插入原图和四点透视校正入口。
- [x] T211 保存原图与校正后的 JPEG，并在 Markdown 中引用校正图。
- [x] T212 提供恢复矩形、拖拽四点、取消回滚和校正失败回滚。
- [ ] T213 分别在 HarmonyOS 4 APK 与 HarmonyOS 5/6 HAP 真机验证拍照、取消、
  裁剪、透视校正、重启和连续多拍。
