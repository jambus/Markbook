# Quickstart: 拍照验收

1. 新建笔记并输入一段正文，将光标放在正文中间。
2. 点击拍照，完成拍摄并返回应用。
3. 确认图片出现在预期位置，正文链接为相对路径。
4. 立即强制结束并重启应用，确认图片仍可打开。
5. 重复拍摄三张，确认顺序、文件名和链接均正确。
6. 分别测试取消、拒绝系统操作和磁盘空间不足。
7. 检查 `attachments/` 与临时目录，不应存在失败操作遗留文件。
8. 在附件移动后、正文保存前强制结束，再启动应用，确认未引用附件被清理。
9. 在正文保存后、事务标记删除前强制结束，确认已引用附件仍被保留。
