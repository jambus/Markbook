# Tasks: 本地 Markdown 笔记本

本清单只跟踪平台无关的行为定义、共享逻辑/样例和最终能力验收。Android APK 的实现
状态由 `005` 跟踪，HAP 的实现状态由 `006` 跟踪；不在本文件重复登记某一端的 UI 或
平台 API 工作。本规格只有两端均通过共享验收后才可标记完成。

## Phase 1 数据可靠性

- [x] T001 定义平台无关的 `Note`、Markdown 文档与可重建索引模型。
- [x] T002 定义选择已有 Obsidian Vault、UTF-8 Markdown 读写和事实源边界。
- [x] T003 定义临时文件、原子替换和失败恢复语义，并纳入共享 Vault 契约。
- [ ] T004 [P] 添加两端共用的空标题、重名和 Unicode 样例与断言。
- [ ] T005 添加两端共用的异常写入恢复用例。

## Phase 2 编辑工作流

- [x] T006 定义笔记列表、创建、打开和编辑的跨端结果与验收场景。
- [x] T007 定义约 600 ms 防抖自动保存及未保存/保存中/失败/成功状态语义。
- [ ] T008 建立回收站浏览、只读查看和永久删除的共享验收；各端实现分别由 `005`、`006` 跟踪。
- [ ] T009 [P] 建立空、加载、读取失败和无障碍的共享验收样例。

## Phase 3 浏览与搜索

- [ ] T010 明确 CommonMark 预览的共享行为与无法保真编辑结构的降级规则。
- [x] T011 建立可从 Markdown 文件重建的标题/正文搜索参考逻辑。
- [x] T012 [P] 建立 1,000 篇笔记、500 ms 上限的共享搜索指标测试。

## Phase 4 验收

- [ ] T013 分别在 HarmonyOS 4 APK 与 HarmonyOS 5/6 HAP 验证选择 Vault、创建
  每日笔记、编辑、重启和恢复。
- [ ] T014 建立清空回收站的共享验收样例，覆盖空回收站、取消、全部成功、部分失败和
  附件保留；Android 与 HAP 分别由 `005`、`006` 跟踪实现。
- [ ] T015 建立文件库管理共享样例：名称合法性/NFC/大小写冲突、`.md` 规范化、原子目录
  移动、深度附件链接和每日目录配置重写；平台实现分别由 `005`、`006` 跟踪。
