# Tasks: 拍照与本地附件

本清单只跟踪平台无关的拍照边界、附件事务、共享测试和最终能力验收。Android APK 的
实现状态由 `005` 跟踪，HAP 的实现状态由 `006` 跟踪；最终验收必须覆盖两端。

- [x] T201 定义两端均调用系统相机入口、应用只接收拍摄结果的边界。
- [x] T202 定义相机返回内容复制到 Vault 后才允许持久引用的规则。
- [x] T203 定义相对 Markdown 链接、原插入位置和图片可验证显示的验收结果。
- [ ] T204 建立拍摄时间加短随机码、真实 MIME/扩展名和旧 UUID 文件兼容的共享样例。
- [x] T205 定义临时目录、校验、事务标记、原子提交和失败回滚流程。
- [x] T206 定义用户取消与真实错误的跨端反馈语义。
- [x] T207 [P] 建立附件命名、路径、格式、插入位置和恢复决策的共享测试。
- [x] T208 定义连续拍摄及按当前光标/选区顺序插入的验收场景。
- [x] T209 [P] 定义启动恢复时只清理未被 Markdown 引用的不完整附件事务。
- [x] T210 定义照片预览、矩形裁剪和四点透视校正的共同交互结果。
- [x] T211 定义原图/校正图成对保存且 Markdown 默认引用校正图的契约。
- [x] T212 定义恢复矩形、拖拽四点、取消和校正失败的回滚验收。
- [ ] T213 分别在 HarmonyOS 4 APK 与 HarmonyOS 5/6 HAP 真机验证拍照、取消、
  裁剪、透视校正、重启和连续多拍。
