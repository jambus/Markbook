# Tasks: HarmonyOS 4 Android APK

## 工程基线

- [x] T501 创建 `android/` Kotlin/Gradle 工程与 Gradle Wrapper。
- [x] T502 配置 application ID、arm64 构建、debug 签名和版本信息。
- [x] T503 使用 Gradle Wrapper 生成 debug APK。
- [ ] T504 在 Mate 60 HarmonyOS 4 完成安装、启动和升级安装。
- [ ] T505 记录可用 Android API、WebView、目录授权、相机 URI 和后台限制。

## 本地笔记

- [x] T506 实现 Vault 选择、持久 URI 权限、重新授权和最近 Vault 恢复。
- [x] T507 实现原子文件仓储、每日笔记和共享路径规则。
- [x] T508 实现所见即所得编辑、600 ms 自动保存和手动保存。
- [ ] T509 实现文件夹浏览、搜索、标签和双向链接基础能力。
- [x] T517 将启动页改为 Vault 文件库，显示当前 Vault、目录结构、Markdown 笔记和
  今日笔记入口。
- [x] T518 实现文件库到编辑器的逐层导航与返回上下文；搜索、标签和双向链接仍由
  T509 跟踪。
- [x] T519 提供设置页的日间/夜间显示模式和 Vault 内每日笔记目录选择。
- [x] T520 根据每日笔记目录深度生成到 `assets/` 的相对 Markdown 图片链接。
- [x] T521 让照片处理页首次进入即显示默认选区，并为矩形与四点校正提供可访问的明确选中态。
- [x] T522 保持照片处理的笔记上下文；成功后在原光标位置插入链接并恢复原阅读位置。
- [x] T523 在照片写入和失败恢复期间提供持续状态、禁用重复插入并保留重试路径。
- [x] T524 在编辑器中展示 Vault/目录上下文，并统一关键反馈的产品视觉语义。
- [x] T525 整理文件库底部操作区，并区分空目录与当前 Vault/目录不可访问状态。
- [x] T526 在设置页显示版本/构建号，以及中文已启用和 English 即将支持状态。
- [ ] T527 [TODO] 国际化：将 Android 文案迁移到资源文件，提供完整中文与英文翻译、
  持久化语言选择，并覆盖文件库、编辑器、相机、同步、错误恢复和辅助技术文案的双语验收。
- [x] T528 将当前 Android APK 功能范围归档为 `0.1.0` 开发基线，并建立持续维护的
  release notes 与版本管理规则。
- [x] T529 Android：实现撤销、重做、标题、加粗、斜体、标签、链接和表格的可访问
  Markdown 工具栏，并保持选区与取消语义。
- [x] T530 Android：在设置页增加回收站项目统计和“清空回收站”；明确不可撤销与附件
  保留，后台逐项删除并显示成功/失败结果和重试入口（FR-013）。
- [x] T531 Android：实现可单测的 Vault 名称策略，覆盖 Unicode、NFC、大小写冲突、`.md`
  规范化、保留名和 UTF-8 长度边界。
- [x] T532 Android：在当前普通目录提供“新建笔记”和“新建文件夹”，对话框行内校验并在
  成功后分别打开笔记或进入文件夹。
- [x] T533 Android：为普通笔记和文件夹增加重命名及整体移到 Vault 根 `.trash/`；非空目录
  仅使用 Provider 原子移动，失败不改变源。
- [x] T534 Android：以真实笔记父目录构建附件路径和 editor context；目录变更使每日目录
  配置无效时重置为 Vault 根并持续提示。
- [x] T535 Android：仓储创建、重命名、移动 API 使用明确 sealed 结果，返回实际名称并对
  Provider 名称改写给出反馈。
- [x] T536 Android：补充名称、路径深度和每日目录改写自动化测试，并运行本地单测、契约
  检查及 debug APK 构建。
- [ ] T537 在 Mate 60 验证新建、重命名、非空目录回收站、Provider 失败、深层附件与每日
  目录重置；未验证前不得标记为候选或已发布。
- [x] T538 Android：为主文件库普通笔记和文件夹实现可中断的左滑“重命名 / 删除”操作行；
  删除仍复用移到回收站确认流程，并保留长按菜单及 TalkBack 自定义操作入口（FR-519）。
- [x] T539 Android：增加纯手势策略单测，覆盖 touch slop、轴锁、位移钳制、44%/45% 阈值、甩动和无动画收敛。
- [ ] T540 在 Mate 60 验证左滑、纵向滚动、返回收起、字体缩放、TalkBack、深浅色和回收站失败恢复；未验证前不得标记为候选或已发布。

## 拍照闭环

- [x] T510 接入系统相机并事务式复制照片到 Vault。
- [x] T511 实现直接插入、矩形裁剪和四点透视校正。
- [x] T512 保存原图与校正图并插入相对 Markdown 链接。
- [x] T513 实现取消、权限拒绝、空间不足、强制结束后的临时文件清理和孤儿附件恢复逻辑。
  真机异常退出验证仍由 T515 覆盖。

## 视频捕获闭环

- [x] T541 Android：以系统 `ACTION_VIDEO_CAPTURE` 实现拍照/录视频显式选择、输出 URI 授权、确认页、180 秒/200 MiB/视频轨校验及取消/重新录制；本地实现与自动化验证完成，真机由 T544 跟踪。
- [x] T542 Android：实现 `-v.{mp4|3gp}` 命名、含尖括号相对 Markdown 链接、持久 pending 会话、最终附件/提交阶段、流式可取消复制、hash 外变保护与 marker 恢复；本地实现与恢复策略验证完成。
- [x] T543 Android：增加视频链接 round-trip、命名冲突、限时/限大小、伪容器拒绝、rename 不确定和保守恢复的纯逻辑/本地回归测试；77 项单测、契约检查和 debug APK 构建已记录通过。
- [ ] T544 Mate 60：验证系统相机录制、取消、重新录制、插入、系统播放器打开、横竖屏、180 秒/200 MiB 边界、强制结束与外部正文变更恢复。

## 验收

- [x] T514 运行 Android 单测和共享格式样例；`0.1.1` 共 33 项本地单测及 Vault 契约检查通过。
- [ ] T515 在 Mate 60 验证竖横屏、字体缩放、重启和连续拍照。
- [ ] T516 输出可安装 APK、版本号、构建记录和真机验收结果。

本机已验证共享 Vault 契约检查与 `android/gradlew --offline --no-daemon assembleDebug` 成功，产物为
`android/app/build/outputs/apk/debug/app-debug.apk`；Mate 60 真机安装与验收仍未完成。
