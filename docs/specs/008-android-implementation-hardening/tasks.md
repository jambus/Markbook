# Tasks: Android 客户端实现加固

依赖顺序自上而下。`[P]` 表示可与同组其他任务并行。

## 阶段 1 · 可验证性

- [x] T801 为 `android/app` 建立 JUnit 单元测试源集与依赖。
- [x] T802 将 `MarkdownCodec` 的 HTML 转义与附件 URL 生成解耦出 Android 框架依赖（FR-818）。
- [x] T803 补充 Markdown 渲染与往返的单元测试，覆盖标题、列表、图片、转义与逐字保留块。

## 阶段 2 · 内容保真

- [x] T804 引入逐字保留的块级结构机制，覆盖围栏代码块、缩进代码块、表格、有序列表、嵌套列表、引用块、水平线、任务列表与 YAML front matter（FR-801、FR-802）。
- [x] T805 序列化链接时使用原始 Markdown 目标，不再取 DOM 的 `href`（FR-803）。
- [x] T806 引入编辑脏标记，未发生用户编辑的笔记不写入（FR-804）。
- [x] T807 编辑器拦截粘贴事件，仅接受纯文本（FR-816）。
- [x] T823 序列化时不再对正文重复 HTML 转义，避免 `<`、`&` 在每次保存后被逐层转义（FR-801）。

## 阶段 3 · 保存可信

- [x] T808 修正自动保存计时基准，使延迟保存真实触发（FR-805）。
- [x] T809 加固保存状态机：编辑器释放时复位挂起状态，队列中的完成回调不再被丢弃（FR-807）。
- [x] T810 将崩溃恢复扩展到用户可编辑的全部目录，并在打开 Vault 时执行一次（FR-806）。

## 阶段 4 · 资源与性能

- [x] T811 编辑器离开时销毁 WebView 实例（FR-812）。
- [x] T812 列表摘要改为有界读取，不再读取整篇笔记（FR-810）。
- [x] T813 [P] 限制拍照预览解码尺寸与位图配置（FR-813）。
- [x] T814 [P] 后台回调增加宿主存活检查（FR-814）。
- [x] T815 将 Vault 目录枚举、笔记读取与正文写入迁出主线程；以单线程写入队列保持同一
  笔记的请求顺序，并让 UI 回调检查当前页面与内容修订号（FR-809、FR-821）。
- [ ] T816 笔记与文件夹列表改用可回收列表容器（FR-811）。

## 阶段 5 · 平台细节

- [ ] T817 [P] 相机输出改用 FileProvider 接口生成 URI 并授权目标应用（FR-815）。
- [x] T818 [P] 编辑器正文字号响应系统字体缩放（FR-817）。
- [ ] T819 实现实例状态保存与恢复，覆盖当前界面、当前笔记与浏览路径（FR-808）。
- [ ] T820 [P] 以常量替换 SAF 权限标志魔数。

## 阶段 6 · 0.1 内容与状态可信优化

### 读取安全

- [x] T824 为笔记打开接口定义“加载中、读取成功、新建成功、读取失败、授权失效”的
  明确结果，移除已有笔记读取失败时的默认正文兜底（FR-819、FR-820）。
- [x] T825 实现编辑器加载和持续错误界面：读取成功前不显示“已保存”且禁用编辑/保存；
  失败时提供“重试”“重新选择 Vault”和“返回文件库”（FR-819、FR-820）。

### 上下文恢复

- [ ] T826 扩展 T819 的实例状态模型，保存并恢复当前页面、Vault 相对路径、当前笔记、
  文件库滚动位置、编辑器阅读位置和可安全恢复的未保存状态（FR-808、FR-822）。
- [ ] T827 保存拍照临时文件标识、所属笔记、插入点、阅读位置、处理模式和角点；旋转或
  重建后恢复处理页，无法恢复时清理临时文件并显示可理解说明（FR-823）。

### 保存可信

- [x] T828 为每次编辑和保存快照分配单调递增修订号；旧请求完成时不得覆盖新内容或把
  最新状态改为“已保存”，保存按钮在写入中防止重复提交（FR-805、FR-807、FR-821）。
- [x] T829 [P] 增加保存状态机单元测试，覆盖保存中继续输入、旧请求晚到、写入失败后继续
  输入、后台触发保存、返回时重试与明确放弃（FR-821、FR-822）。

### 同步可发现

- [ ] T830 将同步任务、取消令牌、阶段、计数和结果迁入独立于 Activity 页面的单一状态
  持有者；页面离开、旋转和 Activity 重建不触发静默取消或重复同步（FR-824）。
- [ ] T831 设置页显示“正在同步/已取消/失败/冲突/上次完成”等持续状态，并提供进入当前
  进度或最近结果的入口（FR-825、FR-826）。
- [ ] T832 [P] 增加同步生命周期测试，覆盖离开进度页、旋转、Activity 重建、明确取消、
  完成和失败后的结果重新进入（FR-824、FR-825、FR-826）。

## 验收

- [ ] T821 在 HarmonyOS 4 Mate 60 真机执行 `quickstart.md` 的全部路径并记录结果。
- [ ] T822 依据 `docs/design/review_checklist.md` 完成一次评审并记录例外。
- [x] T833a 执行 `0.1.1` 的读取状态、并发保存、失败重试、后台保存和全 Vault 恢复策略
  自动化测试，并完成 debug APK 构建；Mate 文档提供方故障注入仍由 T834 验收。
- [ ] T833b 执行三类页面旋转/重建和同步离页恢复的自动化或可重复集成测试，结果关联到
  后续 `0.1.x` 候选版本。
- [ ] T834 在 HarmonyOS 4 Mate 60 上以最大字体和横竖屏完成上述四类体验验收，并将
  设备、系统、APK 版本和例外记录到发布记录。
