# Tasks: Android 客户端实现加固

依赖顺序自上而下。`[P]` 表示可与同组其他任务并行。

## 阶段 1 · 可验证性

- [x] T801 为 `android/app` 建立 JUnit 单元测试源集与依赖。
- [x] T802 将 `MarkdownCodec` 的 HTML 转义与附件 URL 生成解耦出 Android 框架依赖（FR-818）。
- [x] T803 补充 Markdown 渲染与往返的单元测试，覆盖标题、列表、图片、转义与逐字保留块。

## 阶段 2 · 内容保真

- [x] T804 引入逐字保留的块级结构机制，覆盖围栏代码块、缩进代码块、表格、有序列表、嵌套列表、引用块、水平线、任务列表与 YAML front matter（FR-801、FR-802）。
- [x] T805 序列化链接时使用原始 Markdown 目标，不再取 DOM 的 `href`（FR-803）。
- [x] T806 引入编辑脏标记，未发生用户编辑的笔记不写入（FR-804）。
- [x] T807 编辑器拦截粘贴事件，仅接受纯文本（FR-816）。
- [x] T823 序列化时不再对正文重复 HTML 转义，避免 `<`、`&` 在每次保存后被逐层转义（FR-801）。

## 阶段 3 · 保存可信

- [x] T808 修正自动保存计时基准，使延迟保存真实触发（FR-805）。
- [x] T809 加固保存状态机：编辑器释放时复位挂起状态，队列中的完成回调不再被丢弃（FR-807）。
- [ ] T810 将崩溃恢复扩展到用户可编辑的全部目录，并在打开 Vault 时执行一次（FR-806）。

## 阶段 4 · 资源与性能

- [x] T811 编辑器离开时销毁 WebView 实例（FR-812）。
- [x] T812 列表摘要改为有界读取，不再读取整篇笔记（FR-810）。
- [x] T813 [P] 限制拍照预览解码尺寸与位图配置（FR-813）。
- [x] T814 [P] 后台回调增加宿主存活检查（FR-814）。
- [ ] T815 将 Vault 目录枚举、读取与写入迁出主线程（FR-809）。
- [ ] T816 笔记与文件夹列表改用可回收列表容器（FR-811）。

## 阶段 5 · 平台细节

- [ ] T817 [P] 相机输出改用 FileProvider 接口生成 URI 并授权目标应用（FR-815）。
- [x] T818 [P] 编辑器正文字号响应系统字体缩放（FR-817）。
- [ ] T819 实现实例状态保存与恢复，覆盖当前界面、当前笔记与浏览路径（FR-808）。
- [ ] T820 [P] 以常量替换 SAF 权限标志魔数。

## 验收

- [ ] T821 在 HarmonyOS 4 Mate 60 真机执行 `quickstart.md` 的全部路径并记录结果。
- [ ] T822 依据 `docs/design/review_checklist.md` 完成一次评审并记录例外。
