The binary photo fixtures are supplied by device tests. The expected names are
`<uuid>-original.jpg` and `<uuid>-corrected.jpg`; both files must remain in the
the Vault's `attachments/` directory and the corrected file is the default
Markdown reference.
