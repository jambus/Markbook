Binary image fixtures are supplied by device tests. A note named
`2026-01-02.md` stores its pair here as `120000-a1b2-o.jpg` and
`120000-a1b2-c.jpg`; the corrected `-c` file is the default Markdown target.
